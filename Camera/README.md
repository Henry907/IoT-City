# Run script "sh start.sh" as root user (sudo su first)
Runs over HLS  
CANNOT BE RUN AS LBC2 USER or else it will stop when ssh disconnects  
Camera feed path: http://192.168.2.15:8888/cam/index.m3u8
